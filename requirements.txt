# BCM v8.5 — Common dependencies (all platforms)
#
# These are safe to install on any target — no C extensions that need
# SDL / X / GTK. The pygame-based PyGame dashboard renderer is opt-in
# and lives in requirements-x86.txt so armv7l / aarch64 builds that
# use the Flask frontend (--frontend) don't have to build pygame
# from source.
PyYAML>=6.0
pyserial>=3.5

# HTTP client — used by src/trip/route_planner.py for the
# OpenRouteService + TomTom travel-plan calls. The import is wrapped
# in try/except so BCM still boots without it, but the Travel Plan
# feature then silently falls back to the haversine straight-line
# estimate, which is not what the user wants. Pure Python, wheels on
# every architecture.
requests>=2.28
